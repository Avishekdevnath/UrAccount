NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Master frontend toggle for system-admin UI routes and login redirect.
# Keep OFF when rolling back control plane access.
NEXT_PUBLIC_SYSTEM_ADMIN_UI=0

# For local demo only: show "System Admin" demo account pill on /login.
# Keep this OFF in production.
NEXT_PUBLIC_SHOW_SYSTEM_ADMIN_DEMO=0
