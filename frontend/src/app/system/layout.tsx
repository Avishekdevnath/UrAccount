import type { ReactNode } from "react";

export const metadata = {
  title: "System Control Panel · UrAccount",
};

export default function SystemLayout({ children }: { children: ReactNode }) {
  return children;
}
